import ShopSkeleton from "@/features/products/components/ShopWithSidebar/ShopSkeleton";

const Loading = () => {
  return <ShopSkeleton variant="grid" showSidebar={false} />;
};

export default Loading;
